<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/notesSearchRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/primaryShadeDarkColor"
    android:orientation="vertical">

    <!-- Header: back affordance · title -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="14dp"
        android:layout_marginEnd="18dp"
        android:layout_marginTop="14dp"
        android:layout_marginBottom="6dp"
        android:baselineAligned="false"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/notesSearchBack"
            style="@style/TextLarge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/notes_back"
            android:paddingHorizontal="6dp"
            android:paddingVertical="2dp"
            android:text="@string/notes_back_symbol"
            android:textColor="?attr/primaryColor"
            android:textSize="26sp" />

        <TextView
            android:id="@+id/notesSearchTitle"
            style="@style/TextLarge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="4dp"
            android:text="@string/notes_search_title"
            android:textColor="?attr/primaryColor"
            android:textSize="22sp" />

    </LinearLayout>

    <!-- Search bar: input box · magnifier search button -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="12dp"
        android:layout_marginVertical="8dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <EditText
            android:id="@+id/notesSearchInput"
            style="@style/TextSmall"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_weight="1"
            android:background="@drawable/rounded_rect_shade_color"
            android:gravity="center_vertical"
            android:hint="@string/notes_search_hint"
            android:imeOptions="actionSearch"
            android:inputType="text"
            android:maxLines="1"
            android:minHeight="40dp"
            android:paddingHorizontal="16dp"
            android:paddingVertical="8dp"
            android:textColor="?attr/primaryColor"
            android:textColorHint="?attr/primaryColorTrans50"
            android:textCursorDrawable="@null"
            android:importantForAutofill="no"
            tools:ignore="Autofill" />

        <ImageView
            android:id="@+id/notesSearchButton"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:layout_gravity="center_vertical"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/notes_search"
            android:padding="10dp"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_lucide_search"
            app:tint="?attr/primaryColor" />

    </LinearLayout>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipChildren="false"
            android:clipToPadding="false"
            android:fadingEdgeLength="24dp"
            android:paddingTop="8dp"
            android:paddingBottom="8dp"
            android:requiresFadingEdge="vertical"
            tools:listitem="@layout/adapter_notes_item" />

        <TextView
            android:id="@+id/notesSearchEmpty"
            style="@style/TextSmallLight"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginHorizontal="48dp"
            android:gravity="center"
            android:text="@string/notes_search_prompt"
            tools:visibility="visible" />

    </FrameLayout>

</LinearLayout>
